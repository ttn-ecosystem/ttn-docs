# 快速安装

## 安装前置要求

在安装 ttn-cli 之前，请确保你的系统已经安装了以下工具：

- Node.js (版本 >= 16.0.0)
- pnpm (推荐版本 >= 7.0.0)

## 安装步骤

### 1. 全局安装

```bash
pnpm install -g ttn-cli
```

### 2. 验证安装

安装完成后，运行以下命令验证是否安装成功：

```bash
ttn --version
```

如果安装成功，你将看到 ttn-cli 的版本号输出。

## 下一步

安装完成后，你可以查看[使用示例](/usage-examples)了解如何使用 ttn-cli 创建项目。